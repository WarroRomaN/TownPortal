spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/town_portal
    username: postgres
    password: 81868936
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true
    hibernate:
      ddl-auto: update